#!/bin/bash
# GZ Programming Language Compiler
# This script is a wrapper for the GZ compiler

# Get the directory of this script
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

# Run the GZ compiler
python3 "$SCRIPT_DIR/src/gzc.py" "$@"
